class Product {
  int id, price;
  String name, description, status, updatedAt;

  Product({
    required this.id,
    required this.price,
    required this.name,
    required this.description,
    required this.status,
    required this.updatedAt,
  });
}
